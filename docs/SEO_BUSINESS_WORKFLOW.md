# SEO operational workflow

CustomerHub treats SEO data as an operational signal, not an autonomous optimisation engine:

`organisation-scoped Google data → explainable rule → staff review → SEO Request ticket → existing assignment/SLA/audit workflow`

Ticket creation is explicit and staff/admin-only. The created ticket uses the existing ticket creation path, so tenant checks, assignment, SLA and notifications are not duplicated.
